# Minimal Copilot Instructions

Keep guidance minimal and focused.

Follow the window context-scoped design philosophy in `AGENTS.md`:
- Top window = persistent title/branding/actions.
- Left window = global menu context.
- Center window = AI interaction/chat.
- Right window = mode/state-dependent context.

Preserve these responsibilities unless explicitly directed otherwise.
